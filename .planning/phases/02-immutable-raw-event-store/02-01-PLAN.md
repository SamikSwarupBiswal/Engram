---
phase: 02-immutable-raw-event-store
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Engram.Store/**
  - src/Engram.Cli/**
  - tests/Engram.Store.Tests/**
autonomous: true
requirements:
  - REQ-004
  - REQ-005
  - REQ-006
  - NFR-002
must_haves:
  truths:
    - "D-011: Atomic writes via temp file + rename"
    - "D-012: Per-event processing sidecar (.meta.json)"
    - "D-013: Hash verification on read"
    - "D-014: ReplayQuery for filtering by date, source, status"
    - "D-015: CLI filter flags for replay command"
    - "NFR-002: Writes are idempotent and resumable"
    - Raw event payload files are never corrupted by partial writes
    - Processing status is tracked without mutating original payloads
    - Replay supports filtering by date range, source, and status
    - Corrupted files are detected and reported
  artifacts:
    - src/Engram.Store/ProcessingSidecar.cs
    - src/Engram.Store/ReplayQuery.cs
    - src/Engram.Store/RawEventWriter.cs (hardened)
    - src/Engram.Store/ReplayEnumerator.cs (filtered)
    - src/Engram.Cli/Program.cs (filter flags)
---

<objective>
Harden the raw event ledger for atomicity, filtering, integrity verification, and sidecar-based processing status.

Purpose: Make the raw store robust enough for future passive ingestion and reprocessing.

Output: Atomic writer, sidecar tracking, filtered replay, integrity verification, updated CLI.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: ProcessingSidecar model + read/write</name>
  <files>src/Engram.Store/ProcessingSidecar.cs, tests/Engram.Store.Tests/ProcessingSidecarTests.cs</files>
  <action>Create ProcessingSidecar class with fields: processing_status, last_processed_at, processing_error, retry_count. Implement read/write to .meta.json sidecar files. Sidecar is separate from raw event payload.</action>
  <verify>dotnet test</verify>
</task>

<task type="auto">
  <name>Task 2: Atomic writer (temp + rename)</name>
  <files>src/Engram.Store/RawEventWriter.cs, tests/Engram.Store.Tests/RawEventWriterTests.cs</files>
  <action>Modify RawEventWriter to write to .tmp first, then atomic File.Move. Remove processing_status from RawEvent model (moved to sidecar). Ensure partial writes never corrupt final files.</action>
  <verify>dotnet test</verify>
</task>

<task type="auto">
  <name>Task 3: ReplayQuery + filtered enumeration</name>
  <files>src/Engram.Store/ReplayQuery.cs, src/Engram.Store/ReplayEnumerator.cs, tests/Engram.Store.Tests/ReplayEnumeratorTests.cs</files>
  <action>Create ReplayQuery with FromDate, ToDate, Source, ProcessingStatus filters. Update ReplayEnumerator to accept query. Add hash verification on read — corrupted events reported separately.</action>
  <verify>dotnet test</verify>
</task>

<task type="auto">
  <name>Task 4: CLI filter flags</name>
  <files>src/Engram.Cli/Program.cs</files>
  <action>Add --from, --to, --source, --status flags to replay command. Wire to ReplayQuery.</action>
  <verify>dotnet test</verify>
</task>

</tasks>

<verification>
- [ ] dotnet test passes (all existing + new tests)
- [ ] Atomic write: crash simulation leaves no corrupt files
- [ ] Sidecar: processing status tracked without payload mutation
- [ ] Replay filters: date, source, status all work independently and combined
- [ ] Integrity: corrupted file detected on replay
- [ ] CLI: filter flags work
</verification>
