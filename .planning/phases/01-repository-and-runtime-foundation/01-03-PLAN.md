---
phase: 01-repository-and-runtime-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/Engram.Store/**
  - src/Engram.Cli/**
  - tests/Engram.Store.Tests/**
autonomous: true
requirements:
  - REQ-003
  - REQ-004
  - REQ-005
  - REQ-006
  - NFR-002
must_haves:
  truths:
    - REQ-003 is satisfied by a typed raw event model with all required fields.
    - "D-006: Raw event payload files are immutable and append-only under `.engram/raw/YYYY-MM-DD/[event_id].json`."
    - "D-007: Duplicate detection uses deterministic content hashing and must not rewrite existing raw payload files."
    - "D-008: Replay/import enumerates persisted raw events and exposes them to later processing without requiring passive capture sources."
  artifacts:
    - src/Engram.Store
    - src/Engram.Cli
    - tests/Engram.Store.Tests
  key_links:
    - Raw writer uses the workspace initializer paths from Plan 01-02.
    - Replay command reads events written by the raw writer.
---

<objective>
Implement the first append-only raw event ledger and replay command.

Purpose: Give Engram its source-of-truth event store before passive ingestion or wiki memory exists.

Output: Raw event model, append-only writer, deterministic dedupe hash, and replay/import enumeration path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repository-and-runtime-foundation/01-CONTEXT.md
@Artifacts/Product Requirements Document_Engram Full Specification.md
@Artifacts/Engram Implementation Plan.md
@.planning/phases/01-repository-and-runtime-foundation/01-01-SUMMARY.md
@.planning/phases/01-repository-and-runtime-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define raw event schema</name>
  <files>src/Engram.Store/**, tests/Engram.Store.Tests/**</files>
  <read_first>.planning/phases/01-repository-and-runtime-foundation/01-CONTEXT.md, Artifacts/Product Requirements Document_Engram Full Specification.md</read_first>
  <action>Create a typed raw event model with these fields: `event_id`, `event_type`, `captured_at`, `source`, `source_uri`, `active_window`, `text`, `metadata`, `privacy_class`, `hash`, and `processing_status`. Use .NET naming conventions in code while preserving JSON field names as snake_case. Include serialization tests.</action>
  <verify>dotnet test Engram.sln</verify>
  <acceptance_criteria>
    - JSON serialization includes every required snake_case field.
    - Tests cover at least one complete event instance.
  </acceptance_criteria>
  <done>Raw event schema is typed and serializable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement append-only writer and dedupe hash</name>
  <files>src/Engram.Store/**, tests/Engram.Store.Tests/**</files>
  <read_first>src/Engram.Store/**</read_first>
  <action>Implement a raw event writer that writes JSON to `.engram/raw/YYYY-MM-DD/[event_id].json`. Compute a deterministic content hash from stable event content before writing. If an equivalent hash already exists, return an existing/duplicate result without rewriting the existing raw payload. Do not mutate stored raw event files.</action>
  <verify>dotnet test Engram.sln</verify>
  <acceptance_criteria>
    - A new event creates one JSON file in the date folder.
    - Rewriting the same event does not update the existing file timestamp or payload.
    - Hash calculation is deterministic across runs.
  </acceptance_criteria>
  <done>Append-only writer and dedupe behavior are implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Implement replay/import enumeration command</name>
  <files>src/Engram.Store/**, src/Engram.Cli/**, tests/Engram.Store.Tests/**</files>
  <read_first>src/Engram.Cli/**, src/Engram.Store/**</read_first>
  <action>Add store API and CLI support to enumerate raw events from `.engram/raw` in deterministic order. The command should read persisted raw event JSON and print or return enough information for future processors to consume events. Do not implement wiki processing in this phase.</action>
  <verify>dotnet test Engram.sln</verify>
  <acceptance_criteria>
    - Replay enumerates multiple raw events in deterministic order.
    - Replay handles an empty raw store without failing.
    - Replay does not mutate raw event files.
  </acceptance_criteria>
  <done>Replay/import path exists and is tested.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet test Engram.sln` passes.
- [ ] Raw event JSON files are written under `.engram/raw/YYYY-MM-DD/[event_id].json`.
- [ ] Duplicate raw events are detected by hash without rewriting existing payloads.
- [ ] Replay/import enumerates persisted raw events without mutation.
</verification>

<success_criteria>

- Raw event schema, writer, dedupe, and replay are implemented.
- Phase 1 raw-store requirements REQ-003 through REQ-006 are covered.
- Phase decisions D-006, D-007, and D-008 are represented in code and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-and-runtime-foundation/01-03-SUMMARY.md`.
</output>
