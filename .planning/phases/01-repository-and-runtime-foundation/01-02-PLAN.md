---
phase: 01-repository-and-runtime-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/Engram.Store/**
  - src/Engram.Cli/**
  - tests/Engram.Store.Tests/**
autonomous: true
requirements:
  - REQ-002
  - NFR-002
user_setup: []
must_haves:
  truths:
    - REQ-002 is satisfied by an initializer that creates the required `.engram` directory tree.
    - "D-005: The `.engram` workspace includes `raw`, `wiki`, `runs`, `config`, `logs`, and `archives` folders."
    - Initialization is idempotent and safe to run repeatedly.
  artifacts:
    - src/Engram.Store
    - src/Engram.Cli
    - tests/Engram.Store.Tests
  key_links:
    - CLI init command calls the Engram.Store workspace initializer.
    - Tests verify workspace initialization behavior on a temporary directory.
---

<objective>
Implement the `.engram` workspace initializer and minimal configuration model.

Purpose: Create the durable local workspace all later ingestion, wiki, run-log, and archive features rely on.

Output: Store APIs and CLI command for idempotent `.engram` initialization.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repository-and-runtime-foundation/01-CONTEXT.md
@Artifacts/Product Requirements Document_Engram Full Specification.md
@Artifacts/Engram Implementation Plan.md
@.planning/phases/01-repository-and-runtime-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement workspace path model</name>
  <files>src/Engram.Store/**, tests/Engram.Store.Tests/**</files>
  <read_first>.planning/phases/01-repository-and-runtime-foundation/01-CONTEXT.md, Artifacts/Engram Implementation Plan.md</read_first>
  <action>In `Engram.Store`, create a workspace path model for `.engram` with required child folders: `raw`, `wiki`, `runs`, `config`, `logs`, and `archives`. Keep paths configurable by root path so tests use a temp directory and no user profile data.</action>
  <verify>dotnet test Engram.sln</verify>
  <acceptance_criteria>
    - The required folder names are represented as constants or equivalent strongly named members.
    - Tests prove paths are derived from a supplied root.
  </acceptance_criteria>
  <done>Workspace path model exists and is tested.</done>
</task>

<task type="auto">
  <name>Task 2: Implement idempotent initializer and CLI command</name>
  <files>src/Engram.Store/**, src/Engram.Cli/**, tests/Engram.Store.Tests/**</files>
  <read_first>src/Engram.Store/**, src/Engram.Cli/**</read_first>
  <action>Add an initializer API that creates the `.engram` root and all required folders. Add a CLI command such as `engram init --root <path>` or equivalent. Running it twice must not fail or rewrite unrelated files. Do not add capture-source setup in this phase.</action>
  <verify>dotnet test Engram.sln</verify>
  <acceptance_criteria>
    - CLI init can create the required directory tree.
    - Re-running init is idempotent.
    - Tests use temporary directories.
  </acceptance_criteria>
  <done>Initializer works through store API and CLI.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet test Engram.sln` passes.
- [ ] A manual or automated CLI init check creates `.engram/raw`, `.engram/wiki`, `.engram/runs`, `.engram/config`, `.engram/logs`, and `.engram/archives`.
- [ ] Re-running init is idempotent.
</verification>

<success_criteria>

- `.engram` initialization is implemented without passive capture.
- Required workspace folders are created.
- The behavior is tested on a temporary root.
- Phase decision D-005 is represented.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-and-runtime-foundation/01-02-SUMMARY.md`.
</output>
