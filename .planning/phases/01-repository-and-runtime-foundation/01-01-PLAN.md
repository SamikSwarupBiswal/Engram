---
phase: 01-repository-and-runtime-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Engram.sln
  - src/Engram.Store/Engram.Store.csproj
  - src/Engram.Cli/Engram.Cli.csproj
  - tests/Engram.Store.Tests/Engram.Store.Tests.csproj
  - Directory.Build.props
autonomous: true
requirements:
  - REQ-001
  - NFR-003
user_setup: []
must_haves:
  truths:
    - "D-001: Phase 1 combines repository/runtime foundation with the first append-only raw store slice."
    - "D-002: Full PRD scope remains in the roadmap, while Phase 1 only implements local foundation and raw event ledger behavior."
    - REQ-001 and NFR-003 are satisfied by a buildable .NET solution and local test harness.
    - "D-003: .NET/C# is used as the primary stack."
    - "D-004: Replaceable provider interfaces are preserved for later OCR, model, browser automation, and workspace connectors."
    - "D-009: Phase 1 tests run locally without cloud credentials."
    - "D-010: Sensitive capture sources stay disabled because they are out of scope until later consent and ingestion phases."
    - The solution builds and tests without cloud credentials.
  artifacts:
    - Engram.sln
    - src/Engram.Store/Engram.Store.csproj
    - src/Engram.Cli/Engram.Cli.csproj
    - tests/Engram.Store.Tests/Engram.Store.Tests.csproj
  key_links:
    - Engram.Cli references Engram.Store.
    - Engram.Store.Tests references Engram.Store.
---

<objective>
Create the first buildable .NET solution skeleton for Engram.

Purpose: Establish the Windows-first implementation surface and test harness before any data capture or cloud functionality exists.

Output: A solution with store, CLI, and test projects that builds and runs locally.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repository-and-runtime-foundation/01-CONTEXT.md
@Artifacts/Product Requirements Document_Engram Full Specification.md
@Artifacts/Engram Implementation Plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create solution and project layout</name>
  <files>Engram.sln, Directory.Build.props, src/Engram.Store/Engram.Store.csproj, src/Engram.Cli/Engram.Cli.csproj, tests/Engram.Store.Tests/Engram.Store.Tests.csproj</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-repository-and-runtime-foundation/01-CONTEXT.md</read_first>
  <action>Create a .NET solution named `Engram`. Add a class library project `Engram.Store`, a console project `Engram.Cli`, and an xUnit test project `Engram.Store.Tests`. Set shared build properties in `Directory.Build.props` for nullable reference types, implicit usings, deterministic builds, and treating warnings as errors only if the generated skeleton is warning-free.</action>
  <verify>dotnet build Engram.sln</verify>
  <acceptance_criteria>
    - `Engram.sln` exists.
    - `src/Engram.Store/Engram.Store.csproj` exists.
    - `src/Engram.Cli/Engram.Cli.csproj` exists.
    - `tests/Engram.Store.Tests/Engram.Store.Tests.csproj` exists.
    - `dotnet build Engram.sln` succeeds.
  </acceptance_criteria>
  <done>The solution skeleton builds locally.</done>
</task>

<task type="auto">
  <name>Task 2: Wire project references and first smoke test</name>
  <files>src/Engram.Cli/Engram.Cli.csproj, tests/Engram.Store.Tests/Engram.Store.Tests.csproj, tests/Engram.Store.Tests/*</files>
  <read_first>Engram.sln, src/Engram.Store/Engram.Store.csproj</read_first>
  <action>Add a project reference from `Engram.Cli` to `Engram.Store`. Add a project reference from `Engram.Store.Tests` to `Engram.Store`. Add a minimal smoke test proving the test project runs without cloud credentials or external services.</action>
  <verify>dotnet test Engram.sln</verify>
  <acceptance_criteria>
    - `Engram.Cli` references `Engram.Store`.
    - `Engram.Store.Tests` references `Engram.Store`.
    - `dotnet test Engram.sln` succeeds.
  </acceptance_criteria>
  <done>Build and test harness are wired.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build Engram.sln` passes.
- [ ] `dotnet test Engram.sln` passes.
- [ ] No cloud credentials or external services are required.
</verification>

<success_criteria>

- The repo has a buildable .NET solution.
- Store, CLI, and test projects exist in stable paths.
- The first test run succeeds locally.
- Phase decisions D-001, D-002, D-003, D-004, D-009, and D-010 are represented in the implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-and-runtime-foundation/01-01-SUMMARY.md`.
</output>
